// 引入Vue的依赖
import Vue from 'vue'
// 引入VueX
import Vuex from 'vuex'

// 使用VueX
Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
